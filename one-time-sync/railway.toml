[build]
builder = "nixpacks"

[deploy]
startCommand = "bundle exec ruby railway_sync_function.rb"
restartPolicyType = "never"

[env]
# Set these in Railway dashboard
# SIMPRO_TEST_URL
# SIMPRO_TEST_KEY_ID
# HUBSPOT_ACCESS_TOKEN
# CSV_FILE (optional)
# LIMIT (optional)
# START_FROM (optional)

